# ── Python ────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ──────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Downloaded data & generated database ─────────────────
# CSVs can be hundreds of MB — never commit them.
# The DB is tracked via Git LFS and IS committed.
downloads/*.csv
downloads/*.csv.*

# ── Secrets / local config overrides ─────────────────────
.env
.env.local
*.secret

# ── Claude Code internals ────────────────────────────────
.claude/

# ── IDEs ─────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── OS ───────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Local idea backlog (personal notes, not shared) ──────
IDEAS.md

# ── Legacy root files from earlier project iteration ─────
champion_info.json
champion_info_2.json
games.csv
summoner_spell_info.json
league_analysis.png
